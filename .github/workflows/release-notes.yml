# Superseded by helm-chart-ci.yml — remove this file after merge.
name: _deprecated-superseded-by-helm-chart-ci
on:
  workflow_dispatch:
jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by helm-chart-ci.yml"